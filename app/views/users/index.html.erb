<% @shown_users.each do |user| %>
  <%= link_to user_path(user), class: "text-decoration-none" do %>
    <%= render "shared/user_box", :name => user.name, :post_num => user.postsCounter, :photo => user.photo%>
  <% end %>
<% end %>
